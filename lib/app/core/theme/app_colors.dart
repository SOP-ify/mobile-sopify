import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Brand ---
  static const Color primary = Color(0xFF1A6FD4); // Accent / links / terms CTA
  static const Color primaryDeep = Color(0xFF005DA8); // Masuk / Daftar buttons
  static const Color brandNavy = Color(0xFF0B1C30); // "sop" wordmark

  // --- Surfaces ---
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color white = Color(0xFFFFFFFF);
  static const Color fieldFill = Color(0xFFFFFFFF);
  static const Color paleBlueFill = Color(0xFFEFF4FF); // Register password fill
  static const Color welcomeFill = Color(0xFFF1F5FF); // Welcome banner bg
  static const Color iconTileBg = Color(0xFFEBF4FF); // Terms header icon tile

  // --- Text ---
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textBody = Color(0xFF414753);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color label = Color(0xFF374151);
  static const Color hint = Color(0xFF9CA3AF);
  static const Color iconMuted = Color(0xFF9CA3AF);

  // --- Lines ---
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFE5E7EB);

  // --- Status ---
  static const Color success = Color(0xFF10B981);
  static const Color successBg = Color(0xFFE7F7EE);
  static const Color danger = Color(0xFFDC2626);
  static const Color dangerBg = Color(0xFFFDECEC);
  static const Color warning = Color(0xFFB45309); // DRAFT badge text
  static const Color warningBg = Color(0xFFFEF3C7); // DRAFT badge bg

  // --- Navigation ---
  static const Color navInactive = Color(0xFF9CA3AF);

  // --- Neutral ---
  static const Color black = Color(0xFF000000);
}
